---
deployment:
  tasks:
    # One task: each .cpanel.yml task runs in its own shell, so an export on a
    # line of its own would not reach the script. Add SEED_DEMO=1 here for a
    # single deploy when a demo book is wanted, then take it back out.
    # TEMPORARY: one deploy that builds the demo database. Revert to the line
    # below straight after it runs.
    # - export APPROOT=/home/flouruge/industrial_erp && /bin/bash $APPROOT/scripts/cpanel_deploy.sh
    - export APPROOT=/home/flouruge/industrial_erp && /bin/bash $APPROOT/scripts/cpanel_seed_demo_db.sh
